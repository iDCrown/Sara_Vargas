import React from 'react';
// import PropTypes from 'prop-types'

import index from './home.module.scss'

const Index = ({id}) => {
	return (
		<section id={id} className={index.home}>
			<h1>aaa</h1>
		</section>
	)
}

export default Index