import React from 'react';

import about from './about.module.scss';

const About = ({id}) => {
	return (
		<section id={ id } className={about.about}>
			<h1>aaeee</h1>			
		</section>
	)
}

export default About